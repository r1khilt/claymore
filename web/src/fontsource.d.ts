// Fontsource packages ship CSS only (no JS types); declare them so the
// side-effect imports in main.tsx typecheck.
declare module '@fontsource-variable/inter'
declare module '@fontsource/instrument-serif'
